# 02 · MDP 与网格世界（Gridworld）：把“未来”引入 RL

> 从这一章开始，RL 和 Bandit 最大的区别出现了：**你现在的动作会改变未来遇到的状态**。

---

## 0. Bellman 直觉：价值函数为什么能递推

```mermaid
flowchart LR
  S[当前状态 s] -->|做动作 a| R[得到奖励 r]
  R --> Sp[到达下一状态 s']
  Sp -->|未来继续获得价值| V2[折扣后的未来价值 γ·V(s')]
  R --> V1[现在价值的一部分]
  V1 --> V[V(s) = 期望(r + γ·V(s'))]
  V2 --> V
```

## 1. 生动案例：通勤路线选择
你每天从家到公司：
- 走高速：通常快，但有时堵车
- 走地面：更稳定，但可能慢
- 你今天选的路，会影响你到达某个路口的时间，从而影响下一段路的拥堵

这就是“状态转移”。Bandit 没有这件事。

---

## 2. MDP 五件套（其实就 3 个核心）
MDP（马尔可夫决策过程）通常写成 (S, A, P, R, γ)：
- **S 状态**：世界的“当前摘要”
- **A 动作**：你能做的选择
- **P 转移**：做了动作后，世界怎么变
- **R 奖励**：做完得到的反馈
- **γ 折扣**：多重视未来

真正核心是：
> 在状态 s 选择动作 a，环境给奖励 r，并转移到下一状态 s'。

---

## 3. 什么是“马尔可夫”（Markov）性质
直觉版：
> 只要你知道“当前状态”，预测未来就不需要更久远的历史。

比如：
- 如果状态里包含“当前位置+速度”，你不需要过去 10 秒的轨迹
- 但如果状态只包含“当前位置”，可能就不够（隐藏了速度）

当状态不够时，就变成 POMDP（部分可观测），那是更高级话题。

---

## 4. 网格世界（Gridworld）为什么是最好的入门环境
Gridworld 是一个二维格子地图：
- 状态 = 你在哪个格子
- 动作 = 上下左右
- 终点奖励 +1
- 每步小惩罚（比如 -0.01）鼓励更短路径

它的优点：
- 可视化强、直觉强
- 能明确感受到“延迟奖励”
- 非常适合理解价值函数与 Bellman 递推

---

## 5. 价值函数：用“打分”来规划未来
### 状态价值 V(s)
表示：
> 从状态 s 出发，如果你按某个策略走，长期平均能拿多少分

### 动作价值 Q(s,a)
表示：
> 在 s 先做 a，然后继续按策略走，长期平均能拿多少分

为什么 Q 更常用？
- 因为它直接回答“现在该做哪个动作”。

---

## 6. Bellman 直觉（很重要）
Bellman 的核心就是一句话：

> **现在的价值 = 现在的奖励 + 折扣后的下一步价值**

当你理解这句话，后面的 Q-learning/DQN/PPO 的很多式子都会“看起来合理”。

---

## 7. 终止任务 vs 持续任务
- 终止任务（episodic）：到达终点/失败就结束（迷宫、游戏一局）
- 持续任务（continuing）：没有明确终点（流量分配、在线控制）

折扣 γ 在持续任务里通常更重要。

---

## 8. 小练习
1) 把 step_cost 从 -0.01 改成 0，会发生什么？（提示：智能体可能“磨蹭”）
2) 在网格里加墙（不可走格子），最短路径会如何变化？

下一章：`03_tabular_q_learning.md`
